<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="labs.sdm.l0402_settings">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--Main activity-->
        <activity android:name=".MainActivity">

            <!-- This is the entry point to the application -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--Activity to configure the application-->
        <!--Enable up navigation-->
        <activity
            android:name=".SettingsActivity"
            android:label="@string/app_name"
            android:parentActivityName="labs.sdm.l0402_settings.MainActivity">

            <!-- Enable up navigation for backwards compatibility -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="labs.sdm.l0402_settings.MainActivity" />
        </activity>
    </application>

</manifest>